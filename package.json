{
  "name": "",
  "version": "",
  "description": "",
  "homepage": "",
  "dependencies" : {
    "coffee-script"   : "*",
    "mincer"          : "*",
    "autoprefixer"    : "*",
    "jsdom"           : "*",
    "mocha"           : "*",
    "should"          : "*",
    "browserify"      : "*",
    "uglify-js"       : "*",
    "coffeeify"       : "*",
    "clean-css"       : "*",
    "codo"            : "*",
    "stylus"          : "*",
    "ws"              : "*",
    "redis"           : "*",
    "coffee-coverage" : "*"
  },
  "scripts": {
    "docs": "codo source",
    "build": "browserify -t coffeeify tmp.js > bundle.js && uglifyjs bundle.js -o bundle.js",
    "test": "mocha specs/*.coffee specs/**/*.coffee -b --compilers coffee:coffee-script -R list --require should",
    "start": "nodemon app.coffee",
    "cover": "mocha --require lib/coverage-compiler.js should --reporter html-cov specs/*.coffee specs/**/*.coffee > test2.html"
  },
  "engines": {
    "node": ">=0.10"
  }
}
